// "use strict";

// const cleanUpRequest = (request, expect) => {
//   const requestObj = Object.assign({}, request);
//   for (const key in requestObj) {
//     if (Object.hasOwnProperty.call(requestObj, key)) {
//       const element = requestObj[key];
//       if (element === undefined || expect[key] === undefined)
//         delete requestObj[key];
//     }
//   }
//   return requestObj;
// };

// module.exports = cleanUpRequest;
